class Theme:

    def __init__(self):

        # Default Pip-Boy green
        self.primary = (0.2, 1.0, 0.2, 1)

        # Dark background
        self.background = (0.05, 0.05, 0.05, 1)

        # Text color
        self.text = (0.2, 1.0, 0.2, 1)


theme = Theme()
